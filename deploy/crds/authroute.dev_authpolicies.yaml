# Generated by `cargo run -p xtask -- codegen`. Do not edit by hand.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: authpolicies.authroute.dev
spec:
  group: authroute.dev
  names:
    categories: []
    kind: AuthPolicy
    plural: authpolicies
    shortNames:
    - authpol
    singular: authpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetRef.name
      name: Target
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Per-route authentication and authorization for Envoy Gateway routes.
        properties:
          spec:
            description: |-
              `AuthPolicy` — per-route authorization, attached to an `HTTPRoute` (ADR-0002).

              Authorization is expressed as CEL: [`AuthPolicySpec::default_policy`] applies
              to the whole target, with optional ordered [`ExtraPolicy`] overrides matched
              by path (first match wins). A route with no `AuthPolicy` is denied
              (default-deny, ADR-0002 §D2).
            properties:
              defaultPolicy:
                description: |-
                  CEL boolean expression evaluated against the session [`Subject`]; `true`
                  allows, `false` denies. Applies to the whole target unless an
                  [`ExtraPolicy`] matches first (ADR-0002 §D3–D4).
                type: string
              extraPolicy:
                description: |-
                  Ordered path overrides; the first whose `pathRegex` matches the request
                  path supplies the policy, otherwise [`Self::default_policy`] is used.
                items:
                  description: A path-scoped policy override within an [`AuthPolicySpec`].
                  properties:
                    pathRegex:
                      description: |-
                        Regex matched against the request path; the same `regex` engine is used
                        at admission and request time (ADR-0006).
                      type: string
                    policy:
                      description: CEL boolean expression applied when `pathRegex` matches.
                      type: string
                  required:
                  - pathRegex
                  - policy
                  type: object
                type: array
              targetRef:
                description: |-
                  The `HTTPRoute` this policy protects. At most one `AuthPolicy` may target
                  a given route (ADR-0002 §D6).
                properties:
                  group:
                    default: gateway.networking.k8s.io
                    description: API group of the target. Only `gateway.networking.k8s.io` is meaningful.
                    type: string
                  kind:
                    default: HTTPRoute
                    description: Kind of the target. Constrained to the kinds AuthRoute can protect.
                    enum:
                    - HTTPRoute
                    type: string
                  name:
                    description: Name of the target resource in the policy's namespace.
                    type: string
                required:
                - name
                type: object
            required:
            - defaultPolicy
            - targetRef
            type: object
          status:
            description: |-
              Status reported by the controller via Gateway API `PolicyStatus`
              conditions (ADR-0002 §D6, ADR-0006). Populated in M3.
            nullable: true
            properties:
              conditions:
                description: Standard Kubernetes conditions (`Accepted`, `ResolvedRefs`, conflict).
                items:
                  description: A single status condition (a trimmed `metav1.Condition`).
                  properties:
                    message:
                      type: string
                    observedGeneration:
                      format: int64
                      nullable: true
                      type: integer
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        title: AuthPolicy
        type: object
    served: true
    storage: true
    subresources:
      status: {}
